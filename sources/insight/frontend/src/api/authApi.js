export const login = async (_username, _password) => {
    // Placeholder API call for login
    // In a real scenario, use apiClient.post('/auth/login', { username, password })
    return { data: { token: 'sample_token' } };
};

export const logout = async () => {
    // Placeholder for logout request if necessary
    return { status: 'success' };
};
